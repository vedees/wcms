mixin install
  section#install
    .container
      .center
        if (lang === 'ru')
          h2.ui-title-1 Установка
        else
          h2.ui-title-1 Install
      .install__wrapper
        .install-content
          if (lang === 'ru')
            span.ui-title-3 4 Шага для старта
          else
            span.ui-title-3 4 Steps for start
          ul.install-steps__list
            block
          // buttons
          .button-list
            +buttonLink('GitHub', 'https://github.com/vedees/wcms', '_blank')(class="button-default")
            if (lang === 'ru')
              +buttonLink('Скачать', '/ru/download')(class="button-primary")
            else
              +buttonLink('Download', '/download')(class="button-primary")
        .install-banner
          //TODO video
          img(src='/static/img/dashboard.png', alt='WCMS Admin Dashboard')
